import { describe, expect, it } from 'vitest';

// SPEC: placeholder so `npm test` (which fails with no test files) passes in CI
// for the scaffold commit. Replaced by the B12-T engine tests in commit 2.
describe('scaffold', () => {
  it('runs the test suite', () => {
    expect(true).toBe(true);
  });
});
